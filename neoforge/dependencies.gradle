dependencies {
    implementation(libs.balmNeoForge) {
        changing = libs.versions.balm.get().endsWith("SNAPSHOT")
    }
    api(libs.shogiApi)
    jarJar("net.blay09.mods:shogi-api") {
        version {
            prefer libs.shogiApi.get().version
        }
    }
    compileOnly(libs.shogiCommon)
    implementation(libs.shogiNeoForge) {
        changing = libs.versions.shogi.get().contains("SNAPSHOT")
    }
}
